# 2026-08-19

本次同步上游 `deepseek-ai/deepseek-harness` 提交范围：

`99f6f02fecdb7dff40c3fbc9470f5907c29f74ca..141eb6fef83422698aef7a981029e843e8161534`

（即 `0.1.0-rc.7` 之后 → `0.1.0-rc.8` 发布，含 Agent Teams 重命名、reasoning content 修复、SQLite v2 chunk 优化、Web UI 自动打开、配置源所有权更新等）

## 上游提交列表（要点）

范围共 46 个提交，以下为主要提交（按主题归并）：

| 提交 | 说明 |
| --- | --- |
| `141eb6fef` | Merge pull request #2783（release/dsh-0.1.0-rc.8） |
| `f1f7dc36f` | release(dsh): 0.1.0-rc.8 |
| `b862725e7` | Merge pull request #2787（worktree-rename-team，Agent Teams 服务键重命名） |
| `05d64b50d` | Merge pull request #2786（reasoning-content-missing-bug，每轮推理都回传 reasoning content） |
| `a9dc3b7ff` | Merge pull request #2780（brand-guidelines-i18n，品牌指南双语拆分） |
| `6efd8edd0` | Merge pull request #2306（sqlite-v2-chunk-packing，SQLite 持久化布局优化） |
| `1a5e038fb` | Merge pull request #2665（添加 GUIDELINES.md，客户端构建产物校验） |
| `1943a532f` | Merge pull request #2769（web-reference-ui-polish，Web 引用 UI 打磨） |
| `e7d24de36` | Merge pull request #2300（pwsh-persistent-pty，PowerShell 持久 PTY） |
| `f8e4ca0bb` | Merge pull request #2410（auto-open-web-ui，默认自动打开 Web UI） |
| `2bff588f3` | Merge pull request #2756（oxlint-diagnostic-locations，oxlint 诊断位置保留） |
| `e12200501` | Merge pull request #2744（remove-hosted-serial-linux，移除 hosted serial linux CI） |
| `d66841ea3` | feat(web,cli): open the ready Web UI by default（默认自动打开 Web UI） |
| `93b4b98ef` | feat(session): optimize SQLite persistence layout（SQLite v2 chunk 打包优化） |
| `583894f7a` | fix(llm-deepseek): pass reasoning content back on every reasoned turn（每轮推理都回传 reasoning content） |
| `4dd1be1a1` / `cb93304ec` / `0e51d5ffa` | refactor: rename agent teams service key / directories / prefix experimental package names（Agent Teams 重命名） |
| `803a60264` | fix: normalize release bump manifest paths（发布 bump manifest 路径归一化） |

## 对文档的改动

- **part3-core/ch8-boot-chain.md**（对应：配置源所有权更新）
  - 8.2：分层环境快照的安全约束中，`BOOTSTRAP_NAMES` 与 `BOOTSTRAP_PREFIXES` 禁止的变量列表新增"决定由哪个环境程序处理一项操作的"类别，补充 `EDITOR`、`PAGER`、`BROWSER` 三个变量。
- **part3-core/ch9-profile-bundle.md**（对应：Profile/Bundle 措辞澄清）
  - 9.2：原"三种内置 Bundle"改为"默认 Profile 模板使用的组合包"，表格列名由 `Bundle` 改为"组合包"，强调这些是默认 Profile 模板使用的组合包（`@deepseek-ai/dsh-base` / `@deepseek-ai/dsh-web-app` / `@deepseek-ai/dsh-headless`），而非固定内置。
- **part3-core/ch16-preset-goal-subagent.md**（对应：产品子代理命名实例支持）
  - 16.3：产品提供方（`subagent-codex` / `subagent-claude-code`）的可选安装描述更新为支持命名实例——Profile 安装对应 Bundle 后，其 patch 挂载默认实例，Profile 可在 host plane 挂载更多命名实例；两个产品都接受多个唯一的 `providerName` 值（保留 `codex` / `claude-code` 作为默认）；每个 Bundle 把可执行文件选择交给包自有的产品运行时（Codex 运行声明的 wrapper，Claude Code 由锁定的 Agent SDK 选择私有原生可执行文件），不再查询或回退宿主产品命令；Agent Preset 通过 `provider` 与 `toolName` 准确公开单个 agent 所需的已配置实例。
- **part4-web/ch17-web-client.md**（对应：Web 客户端 UI 渲染器包重命名）
  - 17.4：`PLATFORM_MODULES` 种子列表中的 `web-react` 改为 `ui-renderer`；
  - 17.7：Slot 系统三层解耦中的渲染器由 `web-react/src/scoped-slots.tsx` 改为 `ui-renderer`（`packages/client/ui-renderer`）。

## 未影响文档的变更及原因

- **Agent Teams 服务键重命名（`4dd1be1a1` / `cb93304ec` / `0e51d5ffa`）**：实验性 Agent Teams 功能的服务键与目录重命名，书中未描述 Agent Teams 的具体服务键或目录路径，无对应章节需改。
- **reasoning content 每轮回传修复（`583894f7a`）**：`llm-deepseek` 适配器的 bug 修复，确保每轮推理都回传 reasoning content。ch14 已描述 reasoning effort 支持，无需更新适配器内部实现细节。
- **SQLite v2 chunk 打包优化（`93b4b98ef`）**：SQLite 持久化后端内部布局优化（chunk 打包），ch10 描述的是持久化接缝（`session/event` + `session/flush`）而非 SQLite 具体实现，无对应描述需修正。
- **Web UI 默认自动打开（`d66841ea3` / `f8e4ca0bb`）**：CLI 行为变化（`dsh web` 启动后自动在浏览器打开 UI），ch17 描述的是 Web 客户端架构（加载/连接/Slot/RPC），不含 CLI 命令行为描述；ch8 描述启动链但未涉及"是否自动打开浏览器"的 CLI 选项，无处可挂、不强改。
- **品牌指南双语拆分与 GUIDELINES.md（`a9dc3b7ff` / `1a5e038fb`）**：上游仓库内部品牌指南文档拆分与客户端构建产物校验脚本，与书籍内容无关。
- **Web 引用 UI 打磨（`1943a532f`）**：Web UI 组件层细节修复（引用输入与召回排序对齐），ch17 是架构层描述，不含引用 UI 组件实测细节。
- **PowerShell 持久 PTY（`e7d24de36`）**：`dsh-tool-bash-persistent` 的 PowerShell 变体内部实现，书中未覆盖该工具的 PTY 初始化细节。
- **oxlint 诊断位置保留（`2bff588f3`）**：CI lint 脚本修复，非产品行为。
- **移除 hosted serial linux CI（`e12200501`）**：仓库 CI 配置调整，书中无对应细节描述。
- **发布 bump manifest 路径归一化（`803a60264`）**：发布脚本内部修复，不属于书籍章节主题。
- **版本标注（index.md / preface.md / 附录 C 的 `0.1.0-rc.7` 与 commit `99f6f02fe`）**：系书籍写作时的既有元信息，按"不动无关内容"原则保留未动（与 2026-08-17 同步的做法一致）；本次上游已发布 `0.1.0-rc.8`，如需全书统改版本标注可另作一次独立修订。
